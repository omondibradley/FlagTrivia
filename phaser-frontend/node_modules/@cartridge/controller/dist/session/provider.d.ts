import { WalletAccount } from 'starknet';
import { SessionPolicies } from '@cartridge/presets';
import { AddStarknetChainParameters } from '@starknet-io/types-js';
import { ParsedSessionPolicies } from '../policies';
import { default as BaseProvider } from '../provider';
import { AuthOptions } from '../types';
interface SessionRegistration {
    username: string;
    address: string;
    ownerGuid: string;
    transactionHash?: string;
    expiresAt: string;
    guardianKeyGuid: string;
    metadataHash: string;
    sessionKeyGuid: string;
    allowedPoliciesRoot?: string;
}
export type SessionOptions = {
    rpc: string;
    chainId: string;
    policies?: SessionPolicies;
    preset?: string;
    shouldOverridePresetPolicies?: boolean;
    redirectUrl: string;
    disconnectRedirectUrl?: string;
    keychainUrl?: string;
    apiUrl?: string;
    signupOptions?: AuthOptions;
};
export default class SessionProvider extends BaseProvider {
    id: string;
    name: string;
    protected _chainId: string;
    protected _rpcUrl: string;
    protected _username?: string;
    protected _redirectUrl: string;
    protected _disconnectRedirectUrl?: string;
    protected _policies: ParsedSessionPolicies;
    protected _preset?: string;
    protected _keychainUrl: string;
    protected _apiUrl: string;
    protected _publicKey: string;
    protected _sessionKeyGuid: string;
    protected _signupOptions?: AuthOptions;
    private _readyPromise;
    reopenBrowser: boolean;
    constructor({ rpc, chainId, policies, preset, shouldOverridePresetPolicies, redirectUrl, disconnectRedirectUrl, keychainUrl, apiUrl, signupOptions, }: SessionOptions);
    private _setSigningKeys;
    private _resolvePreset;
    private validatePoliciesSubset;
    private padBase64;
    private normalizeSession;
    ingestSessionFromRedirect(encodedSession: string): SessionRegistration | undefined;
    username(): Promise<string | undefined>;
    probe(): Promise<WalletAccount | undefined>;
    connect(): Promise<WalletAccount | undefined>;
    switchStarknetChain(_chainId: string): Promise<boolean>;
    addStarknetChain(_chain: AddStarknetChainParameters): Promise<boolean>;
    disconnect(): Promise<void>;
    private tryRetrieveSessionAccount;
    private clearStoredSession;
}
export {};
